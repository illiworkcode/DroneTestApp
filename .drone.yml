---
kind: pipeline
name: default

steps:
  - name: fetch
    image: alpine/git
    commands:
      - git fetch --tags

  - name: test
    image: alpine:3.9
    commands:
      - tag=`git describe --tags`
      - echo $tag
    when:
      branch:
        - master
trigger:
  branch:
    - master